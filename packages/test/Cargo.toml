[package]
name = "tests"
edition = "2024"
version = "0.1.0"

build = "build.rs"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[build-dependencies]
uniprops-gen = { path = "../uniprops_gen", version = "0.3" }

[[bench]]
name = "decimal_extended_benchmark"
harness = false
